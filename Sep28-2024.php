<?php
    // Initializing a string variable $data with the value "Aptech Learning"
    $data = "Aptech Learning";

    // Outputting the type and value of $data using var_dump, wrapped in <pre> for better formatting
    echo "<pre>";
    var_dump($data);  // Shows the string "Aptech Learning" and its type
    echo "</pre>";

    // Reassigning $data to an integer value
    $data = 15;

    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows the integer 15 and its type
    echo "</pre>";

    // Reassigning $data to a floating-point number
    $data = 22.7;

    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows the float 22.7 and its type
    echo "</pre>";

    // Reassigning $data to a boolean value (true)
    $data = true;

    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows the boolean value true
    echo "</pre>";

    // Reassigning $data to an array of names
    $data = ["Hussain","Aazib","Ahmed","Ahsan","Nazar","Hayan"];

    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows an array of strings and its type
    echo "</pre>";

    // Reassigning $data to an empty string
    $data = "";

    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows an empty string and its type
    echo "</pre>";

    // Reassigning $data to an empty array
    $data = [];
    
    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows an empty array and its type
    echo "</pre>";

    // Reassigning $data to null
    $data = null;
    
    // Outputting the type and value of $data after reassignment
    echo "<pre>";
    var_dump($data);  // Shows the value null and its type
    echo "</pre>";

    // Multi-variable assignment: Assigns the value 10 to $a, $b, and $c in one statement
    // This is equivalent to: $a = 10; $b = 10; $c = 10;
    $a = $b = $c = 10;
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Metadata about the document: character set and responsive viewport settings -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>    
</head>
<body>

<!-- Commented-out example of a <pre> block for displaying formatted text in HTML -->
<!-- <pre>
    This is first line.
    This is second line.
</pre> -->
    
</body>
</html>
